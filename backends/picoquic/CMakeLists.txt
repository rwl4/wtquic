# picoquic backend — not yet present. The backend-owned send descriptor
# queue (plan/commit pull-model shim) lives here, never in the engine.
message(STATUS "wtquic: picoquic backend sources not yet present")
