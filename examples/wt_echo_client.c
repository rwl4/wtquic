/*
 * wt_echo_client — WebTransport echo client over MsQuic.
 *
 * STUB: lands alongside wt_echo_server (connect-by-URL, bidi stream
 * echo roundtrip, datagram roundtrip).
 *
 * Not wired into the build yet.
 */

int main(void)
{
    return 0;
}
