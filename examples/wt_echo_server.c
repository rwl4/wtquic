/*
 * wt_echo_server — WebTransport echo server over MsQuic.
 *
 * STUB: the full ~50-line example lands when the session API and the
 * MsQuic convenience layer (listener + credentials) exist. This
 * example is the API ergonomics gate: if an echo server doesn't fit in
 * about 50 readable lines, the convenience layer is not done.
 *
 * Not wired into the build yet.
 */

int main(void)
{
    return 0;
}
